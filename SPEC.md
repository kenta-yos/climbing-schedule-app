# Go Bouldering 仕様書

仲間内でボルダリングの予定と実績を共有する、モバイル前提の PWA。
本書はコードベースから起こした実装ベースの仕様書です。

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js 14.2 App Router |
| バックエンド | Supabase (PostgreSQL) |
| デプロイ | Vercel / `for-vercel` ブランチ |
| 画面数 | 7 画面 |
| テーブル数 | 8 テーブル |
| タイムゾーン | Asia/Tokyo 固定 |

作成日 2026-09-04 / 対象ブランチ `for-vercel`

---

## 目次

1. [概要](#1-概要)
2. [認証と権限](#2-認証と権限)
3. [画面仕様](#3-画面仕様)
4. [データモデル](#4-データモデル)
5. [業務ルール](#5-業務ルール)
6. [API](#6-api)
7. [データ同期](#7-データ同期)
8. [計測](#8-計測)
9. [非機能](#9-非機能)
10. [既知の課題](#10-既知の課題)

---

## 1. 概要

クライミング仲間の少人数グループが、いつ・どのジムに登りに行くかを共有するためのアプリです。予定を出すと他のメンバーがそれを見てワンタップで合流でき、登り終えたら実績として記録が残ります。蓄積された実績はランキング、月別推移、ジム訪問履歴、メンバー同士のつながりグラフとして可視化されます。

利用者は招待制の固定メンバーで、パスワードはありません。ログイン画面に並ぶユーザーアイコンをタップするだけで本人として振る舞います。この前提が認証設計とアクセス制御のすべてを規定しています。

> **設計の中心概念は「ログ」1本です。**
> 予定と実績は別のテーブルではなく、同じ `climbing_logs` テーブルの `type` カラムで区別されます。予定を実績に昇格させる機能はなく、両者は独立して登録されます。

---

## 2. 認証と権限

ログインは `users` テーブルから取得した一覧の中から自分を選ぶ方式です。選択すると 3 つの Cookie が 30 日間の有効期限で発行され、あわせて Zustand のストア（localStorage、キーは `climbing-user-storage`）にも保存されます。Cookie はサーバー側の本人判定に、ストアは画面上のアイコン表示に使われます。

| Cookie | 用途 | 属性 |
|---|---|---|
| `user_name` | 本人判定。全ページのサーバー側で参照される唯一の識別子 | max-age 30d / SameSite=Lax |
| `user_color` | アバターの背景色 | max-age 30d / SameSite=Lax |
| `user_icon` | アバターの絵文字 | max-age 30d / SameSite=Lax |

### アクセス制御の2段構え

ミドルウェアが `/home` `/dashboard` `/gyms` `/admin` 以下を保護し、Cookie が無ければログイン画面へリダイレクトします。各ページのサーバーコンポーネントでも `lib/auth.ts` の `requireUser()` で同じ判定を再度行います。

管理者判定はこれとは別で、`users` テーブルの特定の `id`（`lib/constants.ts` の `ADMIN_USER_ID`）を引き、その行の `user_name` が Cookie の値と一致するかで判定します（`isAdminUser()`）。管理者だけがお知らせタブ、ユーザー管理タブ、分析ダッシュボードにアクセスできます。分析ダッシュボードは非管理者に対して `notFound()` を返し、存在自体を隠します。

---

## 3. 画面仕様

ボトムナビは 5 タブ構成です。予定入力画面はナビを隠したフルスクリーンで開きます。

### ログイン `/`

- 登録ユーザーを 3 列グリッドで表示し、タップで本人として入場する
- タップ時に `access_logs` へ記録を残し、Cookie とストアを書き込んでトップへ遷移する

### トップ `/home`

- **お知らせバナー** — 表示期限内のお知らせを掲出。2 件目以降は折りたたみ、過去分はダイアログで全件閲覧できる
- **みんなの予定** — 今日以降の予定を日付ごと、さらにジムごとにグループ化して表示。今日はオレンジ、明日は青の枠で強調される
- **参加** — 自分がまだ登録していないジムには参加ボタンが出る。時間帯と「ご飯にも行く」を選んでその場で予定が作られる
- **New バッジ** — 前回閲覧時刻を localStorage に持ち、それ以降に他人が作成した予定に赤バッジを付ける
- **ランキング** — 今月・先月タブ切り替え。同数は同順位。名前をタップすると当月のジム別内訳と日付リストが開く
- **バイトシフト** — 特定の 1 ユーザーにのみ表示される登録フォーム。登録するとフィードに緑のシフトカードが並び、他メンバーはそこから合流できる
- 引っ張って更新（しきい値 72px）、30 秒間隔の自動ポーリング、タブ復帰時の再取得に対応

### 予定・実績の入力 `/home/plan`

- 日付、時間帯（昼／夕方／夜）、ジムを選び、**「登るよ（予定）」** または **「登った！」（実績）** で登録する
- ジムは検索で絞り込むか、直近 30 日の実績から作られる「よく行くジム」（最大 6 件）から選ぶ
- **ジム未定** で登録でき、フィードでは「登る仲間を募集中！」として紫枠で目立つように表示される
- 一緒に登るメンバーを選ぶと、そのメンバーの分のログも同時に作成される。アプリ外の友人はフラグとして記録する
- 「ご飯にも行く」「コンペ」のフラグを付けられる
- `?editId=` 付きで開くと編集モードになる。自分のログ以外は編集対象として拒否される
- 編集時、同じ日・ジム・時間帯の他メンバーのログも一括で更新される。既存メンバーはロックされ外せない

### マイページ `/dashboard`

- **プロフィール** — 先月と今月のランキング順位を並べて表示する
- **今後の予定** — 既定 3 件表示。各行から編集・削除ができる
- **月別推移** — 棒グラフ。バーをタップするとその月のジム別内訳が開く
- **ジム訪問履歴** — 回数順・最終訪問順で並べ替え。30 日以上空くと琥珀色、60 日以上で赤のバッジが付く
- **マイデータ管理** — 自分の実績を一覧し個別に削除できるアコーディオン

### つながり `/graph`

- メンバーを円周上に配置し、一緒に登った回数を線の太さで表す SVG グラフ。自分は常に真下に固定される
- 期間は今月・先月・3ヶ月・6ヶ月・1年から選ぶ
- 他人のノードをタップすると、その人の次の予定、最後に一緒に登った日、よく行くジム、よく一緒に行くジムが出る
- 自分をタップすると、その期間に一緒に登った回数のランキングが出る
- 線上の回数バッジをタップすると、2 人のセッション履歴が月別のボトムシートで開く

### ジム `/gyms`

- 開いた瞬間に GPS を取得し、近い順にジムを並べる。出発地は住所や駅名でも指定できる（国土地理院の住所検索 API を利用）
- 並び替えは **近い順・ご無沙汰順** の 2 タブ。ご無沙汰順では未訪問ジムを別枠に分ける
- 既定の表示範囲は「都内・神奈川」。チェックで全国に広がる
- カードには最終登攀日、距離、SNS リンク、未訪問とごぶさたのバッジが載る
- 他人の予定は扱わない。誰がいつどこに行くかはトップの予定フィードで見る
- 読み込むのは自分の実績だけ。全ユーザーのログは取得しない
- 8 件ずつの無限スクロール

### 管理 `/admin`

- **マイ設定** — 全員が使える。自分の名前・アイコン・背景色を変更する。名前を変えると参照している 6 テーブルも追従し、Cookie とストアのログイン情報も更新される
- **ジム登録** — ジム名、URL、エリア、住所がすべて必須。URL は Instagram を基本とし、無ければ X や Facebook、公式サイトでもよい。住所は候補サジェストから確定し、緯度経度に変換して保存される
- **お知らせ**（管理者のみ）— 内容と表示終了日を指定して掲出する
- **ユーザー**（管理者のみ）— ユーザーの追加、アイコンと色の変更、削除。ここからの改名はできない
- ログアウトボタン。Cookie を失効させストアを空にする

### 分析ダッシュボード `/admin/analytics`

- 管理者専用。非管理者には 404 を返す
- **ログ** — 直近 48 時間の生イベント
- **記録ログ** — 予定と実績の作成・更新・削除・参加の履歴（30 日分）
- **アクション** — ページ別 PV と、画面ごとの操作別カウント
- **ユーザー** — アクセス数、最終アクセス日、ページ別 PV の一覧
- 集計対象から管理者自身の行は除外される

---

## 4. データモデル

Supabase の PostgreSQL に 8 テーブル。外部キー制約は使わず、ユーザーとジムはいずれも **名前の文字列で参照** されます。

ユーザー名の変更については、`PUT /api/users` が以下 6 テーブルを順に書き換えて追従させます。トランザクションではないため、途中で失敗した場合は失敗したテーブル名を返します。

| テーブル | カラム |
|---|---|
| `climbing_logs` | `user` |
| `work_shifts` | `user_name` |
| `page_views` | `user_name` |
| `access_logs` | `user_name` |
| `gym_master` | `created_by` |
| `release_announcements` | `created_by` |

### `climbing_logs` — 予定と実績。アプリの中心テーブル

| カラム | 型 | 内容 |
|---|---|---|
| `id` | uuid | 主キー |
| `date` | date | 登る日 |
| `gym_name` | text | ジム名。未定の場合は文字列「ジム未定」が入る |
| `user` | text | ユーザー名 |
| `type` | text | 「予定」または「実績」 |
| `time_slot` | text | 「昼」「夕方」「夜」 |
| `with_friends` | bool | アプリ外の友人と行くか |
| `join_dinner` | bool | 食事にも参加するか |
| `is_comp` | bool | コンペか |
| `created_at` | timestamptz | New バッジの判定に使われる |

### `users` — メンバー。認証情報は持たない

| カラム | 型 | 内容 |
|---|---|---|
| `id` | uuid | 管理者判定にのみ使われる |
| `user_name` | text | 表示名かつ全テーブルでの参照キー |
| `color` | text | アバター背景の HEX カラー |
| `icon` | text | アバターの絵文字 |

### `gym_master` — ジム。緯度経度は距離計算に必須

| カラム | 型 | 内容 |
|---|---|---|
| `gym_name` | text | 主キー相当 |
| `profile_url` | text | Instagram などの SNS リンク |
| `area_tag` | text | `area_master` への参照 |
| `lat` / `lng` | float8 | null だと距離順で末尾に回る |
| `created_by` | text | 登録者 |

### その他のテーブル

| テーブル | 主なカラム | 役割 |
|---|---|---|
| `area_master` | `area_tag`, `major_area` | エリアタグを 4 つの大区分（都内・神奈川／関東／関西／全国）に束ねる |
| `work_shifts` | `user_name`, `gym_name`, `date`, `start_time`, `end_time` | バイトシフト。トップのフィードに合流ポイントとして出る |
| `release_announcements` | `content`, `display_until`, `created_by` | お知らせ。`display_until` を過ぎるとバナーから消える |
| `page_views` | `user_name`, `page`, `action`, `created_at` | PV と操作の記録。分析ダッシュボードの唯一のデータ源 |
| `access_logs` | `user_name`, `created_at` | ログイン時に書き込まれるが、読み出している画面は無い |

> **`set_schedules` テーブルはデータベース上に残っています。**
> セット替え日の登録が運用されなかったため、機能ごとアプリから削除しました。テーブルとデータはそのままで、アプリからは一切参照していません。

---

## 5. 業務ルール

### 重複登録の禁止

条件は **ユーザー・日付・時間帯・種別** の 4 つで、この組み合わせにつき 1 件までです。同じ時間帯に複数のジムへは行けないため、ジム名は条件に含めません。

| 経路 | 判定 |
|---|---|
| フィードの参加パネル | `checkDuplicateLog` でデータベースへ問い合わせ |
| 予定入力画面 | `checkDuplicateLog` でデータベースへ問い合わせ |
| `POST /api/logs` | 同じ条件でデータベースへ問い合わせ、409 を返す |

一緒に登るメンバーを選んだときの重複チェック（`getCompanionConflicts`）も同じ条件です。

編集時も同じ条件で検証します。編集中のログ自身と、一緒に日付や時間帯を動かすメンバーのログは、衝突判定から除きます。検証はすべて書き込みの前に済ませるため、途中で弾かれてもデータは変更されません。

### ジム一覧のバッジ判定

| バッジ | 条件 | 基準日 |
|---|---|---|
| 🆕 未訪問 | 自分の実績が 1 件も無い | — |
| ⌛ ごぶさた | 最終訪問から 30 日以上 | 今日 |

### つながりグラフの辺

実績ログを「日付とジム名」でグループ化し、同じグループに 2 人以上いれば全組み合わせに辺を張ります。回数は共に登ったセッション数です。**時間帯は見ていません。** 同じ日の同じジムに昼と夜で別々に行った 2 人も、一緒に登ったものとして数えられます。

### ランキング

その月の実績件数で並べ、同数は同順位（次の順位は人数分飛ぶ）。集計対象は実績のみで、予定は含みません。

---

## 6. API

データアクセスには 2 経路あります。サーバーコンポーネントとルートハンドラは Cookie 対応の Supabase クライアントを使い、クライアントコンポーネントは `lib/supabase/queries.ts` のブラウザクライアントから直接テーブルを叩きます。

> **`POST /api/logs` を呼び出しているコードはアプリ内にありません。**
> ログの書き込みはすべてブラウザから Supabase を直接叩く経路です。そのため、このエンドポイントの重複チェックと `page_views` への記録は一度も実行されていません。実際に効いているのはクライアント側の 2 つの検証です。

| エンドポイント | メソッド | 内容 |
|---|---|---|
| `/api/logs` | GET / POST | `mode=home` で今日以降の予定と先月以降の実績を返す。`user` で絞り込み。POST は重複時に 409 を返し、成功時に `page_views` へ記録する |
| `/api/users` | GET / POST / PUT / DELETE | ユーザーの一覧・追加・削除。PUT は色とアイコンに加え `new_user_name` による改名に対応し、参照テーブルを追従させる。名前が衝突すると 409 |
| `/api/gyms` | GET / POST / PUT | ジムの一覧・追加・SNS URL とエリアの変更 |
| `/api/shifts` | GET / POST / PUT / DELETE | 今日以降のシフトを返す。追加・時刻変更・削除 |
| `/api/announcements` | GET / POST / DELETE | 既定は表示期限内のみ。`all=true` で全件 |

> **どのエンドポイントも呼び出し元を検証しません。**
> ユーザーの削除もお知らせの投稿も、認証ヘッダや管理者チェック無しで通ります。管理者判定は UI の出し分けだけに使われています。

---

## 7. データ同期

全ページが `force-dynamic` で、リクエストごとにサーバーで取得した初期データを持って描画されます。その後トップ画面だけがクライアント側で鮮度を保ち続けます。

| タイミング | 動作 | 補足 |
|---|---|---|
| 初回描画 | サーバー取得 | 各ページのサーバーコンポーネントが必要なテーブルを並列に読む |
| マウント直後 | 即時再取得 | 編集画面から戻ったときに古い内容を見せないため |
| 30 秒ごと | ポーリング | タブが非表示のあいだは停止する |
| タブ復帰時 | 再取得 | `visibilitychange` を監視 |
| 手動 | 引っ張って更新 | 72px 引くと発火。減衰付きの手応え |
| 書き込み後 | キャッシュ無効化 | Server Action がトップと予定入力のキャッシュを破棄する |

> **楽観的更新もリアルタイム購読も使っていません。**
> 他メンバーの登録が画面に出るまで最大 30 秒かかります。Supabase Realtime は導入されていません。

---

## 8. 計測

すべての計測が `page_views` の 1 テーブルに集まります。書き込みは投げっぱなしで、失敗しても画面には影響しません。`action` が空の行はページビュー、値が入っている行は操作です。操作は `アクション名|日付|ジム名` のようにパイプ区切りで詰め込まれ、集計時に先頭要素だけを取り出して分類されます。

| 画面 | 記録される操作 |
|---|---|
| `home` | 記録ボタン、参加ボタン、参加確定、編集ボタン、シフトへの合流 |
| `plan` | 予定作成、実績登録、予定更新、予定削除、ジムの選び方（検索／よく行く／未定） |
| `gyms` | 並び替えタブ、GPS の自動取得とボタン操作、住所の手入力、全国表示の切り替え |
| `dashboard` / `graph` / `admin` | ページビューのみ |

---

## 9. 非機能

| 項目 | 内容 |
|---|---|
| PWA | standalone 表示、縦向き固定、開始 URL は `/home`。Service Worker は開発時は無効。静的 JS と RSC ペイロードは Network First でキャッシュする |
| iOS 対応 | ノッチと Dynamic Island、ホームバーに合わせて safe-area を全画面で確保。ズーム禁止 |
| 日時 | date-fns-tz で Asia/Tokyo に固定。変換は `lib/utils.ts` に集約し、サーバー・クライアント双方から利用する |
| 表示幅 | 本文は最大 512px の 1 カラム。モバイル専用でデスクトップ最適化は無い |
| 配色 | オレンジからマゼンタへのグラデーションがブランド色。ダークモード非対応 |
| フォント | Noto Sans JP を next/font でセルフホスト |
| 外部依存 | 住所検索は国土地理院のアドレス検索 API をブラウザから直接呼ぶ。API キー不要、レート制限の考慮は無い |
| テスト | テストコード、Lint の CI、型チェックの CI いずれも無し |

---

## 10. 既知の課題

コードを読んで見つかった論点です。上ほど影響が大きい順に並べています。仲間内クローズド運用という前提では許容されているものも含みます。

### 重大 — Cookie を書き換えれば誰にでもなりすませる

本人判定は `user_name` Cookie の文字列比較だけです。署名もセッションもありません。ブラウザの開発者ツールで値を管理者の名前に変えれば、分析ダッシュボードもユーザー管理も開けます。

### 重大 — 書き込み系 API に権限チェックが無い

ユーザー削除もお知らせ投稿も、ログインしていない状態の `curl` で通ります。加えてクライアント側は Supabase を直接叩くため、匿名キーを使って任意のログを作成・削除できます。データベース側の行レベルセキュリティが実際にどう設定されているかは、この仕様書の作成範囲では未確認です。**ここは実機で確認する価値があります。**

### 中 — 重複チェックの条件が 3 箇所で食い違う

フィードからの参加はジム名を含めて判定し、入力画面と API は含めません。同じ日の同じ時間帯に複数のジムを登録できてしまう経路があります。詳細は[業務ルール](#5-業務ルール)の節に記載しました。

### 中 — 個人名がソースコードに埋め込まれている

バイトシフト機能はユーザー名とジム名を定数で直書きしており、その 1 人にしか出ません。管理者判定の UUID も同様に直書きです。メンバーが入れ替わるとコード修正が必要になります。

### 中 — 月別推移グラフの開始年が固定

グラフは 2026 年 1 月から現在月までを描く実装です。年をまたぐと横軸が際限なく伸び、2027 年以降は月ラベルが重複します。

### 軽微 — ジム名の変更が過去データを壊す

全テーブルが名前の文字列で参照し合っています。ユーザー名の変更は API 側で参照テーブルを追従させますが、ジム名には同じ仕組みがありません。

ユーザー名の追従もトランザクションではありません。途中で失敗すると、一部のログだけが旧名のまま残ります。その場合は同じ名前で保存し直せば復旧します。

### 軽微 — 計測テーブルへの書き込みが読まれていない

ログイン時に書き込む `access_logs` テーブルは、読み出す側がありません。

デッドコードは削除済みです。未参照コンポーネント 4 つ（予定入力フォームの旧モーダル版と、shadcn/ui 由来の radio-group・separator・skeleton）、`lib` の未参照 export 18 件、未使用 npm パッケージ 6 つ（`swr` と Radix の dropdown-menu・label・radio-group・separator・toggle）、そしてセット替え機能一式（`/schedule` 画面、管理画面のセット登録タブ、ジム一覧の新セット順ソートとセット関連表示）。

### 軽微 — 一覧系の画面がログを期間で絞らずに読む

マイページとつながりの画面は、期間を絞らずに、あるいは 12 ヶ月分をまとめて `climbing_logs` を取得します。件数が増えるほど初期表示が遅くなります。ジム画面は自分の実績だけを読むよう修正済みです。
